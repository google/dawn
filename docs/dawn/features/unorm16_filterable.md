# Unorm16 filterable

This optional feature makes `R16Unorm`, `RG16Unorm` and `RGBA16Unorm` filterable.
It is needed to allow Graphite to sample (with filtering) individual planes of HDR YUV formats that have planes with formats `R16Unorm` and `RG16Unorm`.
