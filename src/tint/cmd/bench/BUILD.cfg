{
    "bench": {
        /* This is the benchmark executable target for Tint.
           Depend on all the benchmark targets to pull them all together. */
        "AdditionalDependencies": ["**:bench"]
    }
}
