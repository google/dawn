{
    "test_cmd": {
        "OutputName": "tint_unittests",
        /* This is the unittests executable target for Tint.
           Depend on all the test targets to pull them all together. */
        "AdditionalDependencies": ["**:test"]
    }
}
