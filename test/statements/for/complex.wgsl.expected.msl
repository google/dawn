#include <metal_stdlib>

using namespace metal;
void some_loop_body() {
}

void f() {
  int j = 0;
  for(int i = 0; ((i < 5) && (j < 10)); i = (i + 1)) {
    some_loop_body();
    j = (i * 30);
  }
}

