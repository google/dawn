#include <metal_stdlib>

using namespace metal;
struct S {
  /* 0x0000 */ float4x4 a;
};

void foo(device S* const tint_symbol) {
  (*(tint_symbol)).a = ((*(tint_symbol)).a + float4x4(float4(0.0f), float4(0.0f), float4(0.0f), float4(0.0f)));
}

