#include <metal_stdlib>
using namespace metal;

struct str {
  array<int, 4> arr;
};

void func(thread array<int, 4>* const pointer) {
  (*pointer) = array<int, 4>{};
}

[[max_total_threads_per_threadgroup(1)]]
kernel void v() {
  str F = {};
  (func((&F.arr)));
}
