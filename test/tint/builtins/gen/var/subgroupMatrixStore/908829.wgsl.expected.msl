#include <metal_stdlib>
using namespace metal;

template<typename T, size_t N>
struct tint_array {
  const constant T& operator[](size_t i) const constant { return elements[i]; }
  device T& operator[](size_t i) device { return elements[i]; }
  const device T& operator[](size_t i) const device { return elements[i]; }
  thread T& operator[](size_t i) thread { return elements[i]; }
  const thread T& operator[](size_t i) const thread { return elements[i]; }
  threadgroup T& operator[](size_t i) threadgroup { return elements[i]; }
  const threadgroup T& operator[](size_t i) const threadgroup { return elements[i]; }
  T elements[N];
};

struct SB_RW {
  /* 0x0000 */ tint_array<float, 1024> arg_0;
};

struct tint_module_vars_struct {
  device SB_RW* sb_rw;
};

void subgroupMatrixStore_908829(tint_module_vars_struct tint_module_vars) {
  int arg_1 = 1;
  simdgroup_float8x8 arg_2 = make_filled_simdgroup_matrix<float, 8, 8>(0.0f);
  int arg_3 = 8;
  uint const v = max(as_type<uint>(arg_3), 8u);
  uint const v_1 = as_type<uint>(arg_1);
  bool const v_2 = (((v_1 + (v * 7u)) + 8u) <= 1024u);
  (simdgroup_store(arg_2, (&(*tint_module_vars.sb_rw).arg_0[select(0u, v_1, v_2)]), ulong(select(8u, v, v_2)), ulong2(0ul), true));
}

[[max_total_threads_per_threadgroup(1)]]
kernel void compute_main(device SB_RW* sb_rw [[buffer(0)]]) {
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.sb_rw=sb_rw};
  (subgroupMatrixStore_908829(tint_module_vars));
}
