//
// fragment_main
//
#include <metal_stdlib>

using namespace metal;
half4 subgroupInclusiveAdd_58ea3d() {
  half4 res = simd_prefix_inclusive_sum(half4(1.0h));
  return res;
}

fragment void fragment_main(device half4* tint_symbol [[buffer(0)]]) {
  *(tint_symbol) = subgroupInclusiveAdd_58ea3d();
  return;
}

//
// compute_main
//
#include <metal_stdlib>

using namespace metal;
half4 subgroupInclusiveAdd_58ea3d() {
  half4 res = simd_prefix_inclusive_sum(half4(1.0h));
  return res;
}

kernel void compute_main(device half4* tint_symbol [[buffer(0)]]) {
  *(tint_symbol) = subgroupInclusiveAdd_58ea3d();
  return;
}

