#include <metal_stdlib>
using namespace metal;

struct tint_module_vars_struct {
  device array<half, 1024>* prevent_dce;
};

simdgroup_half8x8 subgroupMatrixScalarAdd_38dfbe() {
  simdgroup_half8x8 v = make_filled_simdgroup_matrix<half, 8, 8>(0.0h);
  (simdgroup_multiply_accumulate(v, make_filled_simdgroup_matrix<half, 8, 8>(0.0h), simdgroup_half8x8(1.0h), make_filled_simdgroup_matrix<half, 8, 8>(8.0h)));
  simdgroup_half8x8 res = v;
  return res;
}

[[max_total_threads_per_threadgroup(1)]]
kernel void compute_main(device array<half, 1024>* prevent_dce [[buffer(0)]]) {
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.prevent_dce=prevent_dce};
  (simdgroup_store(subgroupMatrixScalarAdd_38dfbe(), (&(*tint_module_vars.prevent_dce)[0]), ulong(16u), ulong2(0ul), false));
}
