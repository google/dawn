#include <metal_stdlib>

using namespace metal;
float4 fwidth_d2ab9a() {
  float4 res = fwidth(float4(1.0f));
  return res;
}

fragment void fragment_main(device float4* tint_symbol [[buffer(0)]]) {
  *(tint_symbol) = fwidth_d2ab9a();
  return;
}

