#include <metal_stdlib>

using namespace metal;
half2 subgroupMin_d85be6() {
  half2 res = simd_min(half2(1.0h));
  return res;
}

fragment void fragment_main(device half2* tint_symbol [[buffer(0)]]) {
  *(tint_symbol) = subgroupMin_d85be6();
  return;
}

kernel void compute_main(device half2* tint_symbol_1 [[buffer(0)]]) {
  *(tint_symbol_1) = subgroupMin_d85be6();
  return;
}

