//
// main0
//
#include <metal_stdlib>

using namespace metal;
struct tint_symbol {
  int value [[color(0)]];
};

int main0_inner() {
  return 1;
}

fragment tint_symbol main0() {
  int const inner_result = main0_inner();
  tint_symbol wrapper_result = {};
  wrapper_result.value = inner_result;
  return wrapper_result;
}

//
// main1
//
#include <metal_stdlib>

using namespace metal;
struct tint_symbol {
  uint value [[color(1)]];
};

uint main1_inner() {
  return 1u;
}

fragment tint_symbol main1() {
  uint const inner_result = main1_inner();
  tint_symbol wrapper_result = {};
  wrapper_result.value = inner_result;
  return wrapper_result;
}

//
// main2
//
#include <metal_stdlib>

using namespace metal;
struct tint_symbol {
  float value [[color(2)]];
};

float main2_inner() {
  return 1.0f;
}

fragment tint_symbol main2() {
  float const inner_result = main2_inner();
  tint_symbol wrapper_result = {};
  wrapper_result.value = inner_result;
  return wrapper_result;
}

//
// main3
//
#include <metal_stdlib>

using namespace metal;
struct tint_symbol {
  float4 value [[color(3)]];
};

float4 main3_inner() {
  return float4(1.0f, 2.0f, 3.0f, 4.0f);
}

fragment tint_symbol main3() {
  float4 const inner_result = main3_inner();
  tint_symbol wrapper_result = {};
  wrapper_result.value = inner_result;
  return wrapper_result;
}

//
// main4
//
#include <metal_stdlib>

using namespace metal;
struct tint_symbol {
  half value [[color(4)]];
};

half main4_inner() {
  return 2.25h;
}

fragment tint_symbol main4() {
  half const inner_result = main4_inner();
  tint_symbol wrapper_result = {};
  wrapper_result.value = inner_result;
  return wrapper_result;
}

//
// main5
//
#include <metal_stdlib>

using namespace metal;
struct tint_symbol {
  half3 value [[color(5)]];
};

half3 main5_inner() {
  return half3(3.0h, 5.0h, 8.0h);
}

fragment tint_symbol main5() {
  half3 const inner_result = main5_inner();
  tint_symbol wrapper_result = {};
  wrapper_result.value = inner_result;
  return wrapper_result;
}

