#include <metal_stdlib>

using namespace metal;
kernel void tint_symbol(const constant float3x2* tint_symbol_1 [[buffer(0)]], device float3x2* tint_symbol_2 [[buffer(1)]]) {
  float3x2 const x = *(tint_symbol_1);
  *(tint_symbol_2) = x;
  return;
}

