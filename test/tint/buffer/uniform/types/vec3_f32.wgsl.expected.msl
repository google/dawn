#include <metal_stdlib>

using namespace metal;
kernel void tint_symbol(const constant packed_float3* tint_symbol_1 [[buffer(0)]], device packed_float3* tint_symbol_2 [[buffer(1)]]) {
  float3 const x = float3(*(tint_symbol_1));
  *(tint_symbol_2) = packed_float3(x);
  return;
}

