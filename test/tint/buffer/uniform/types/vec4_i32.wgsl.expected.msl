#include <metal_stdlib>

using namespace metal;
kernel void tint_symbol(const constant int4* tint_symbol_1 [[buffer(0)]], device int4* tint_symbol_2 [[buffer(1)]]) {
  int4 const x = *(tint_symbol_1);
  *(tint_symbol_2) = x;
  return;
}

