#include <metal_stdlib>
using namespace metal;

struct tint_packed_vec3_f16_array_element {
  /* 0x0000 */ packed_half3 packed;
  /* 0x0006 */ array<int8_t, 2> tint_pad;
};

struct tint_module_vars_struct {
  const constant array<array<tint_packed_vec3_f16_array_element, 2>, 4>* u;
  device half* s;
};

[[max_total_threads_per_threadgroup(1)]]
kernel void f(const constant array<array<tint_packed_vec3_f16_array_element, 2>, 4>* u [[buffer(0)]], device half* s [[buffer(1)]]) {
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.u=u, .s=s};
  array<tint_packed_vec3_f16_array_element, 2> const v = (*tint_module_vars.u)[2];
  half3x2 const t = transpose(half2x3(half3(v[0u].packed), half3(v[1u].packed)));
  half const l = length(half3((*tint_module_vars.u)[0][1].packed).zxy);
  half const a = abs((*tint_module_vars.u)[0][1].packed.z);
  half const v_1 = half(a);
  (*tint_module_vars.s) = ((v_1 + half(l)) + t[0].x);
}
