#include <metal_stdlib>
using namespace metal;

struct tint_module_vars_struct {
  const constant array<half4x4, 4>* a;
  device half* s;
  thread int* counter;
};

int i(tint_module_vars_struct tint_module_vars) {
  (*tint_module_vars.counter) = as_type<int>((as_type<uint>((*tint_module_vars.counter)) + 1u));
  return (*tint_module_vars.counter);
}

[[max_total_threads_per_threadgroup(1)]]
kernel void f(const constant array<half4x4, 4>* a [[buffer(0)]], device half* s [[buffer(1)]]) {
  thread int counter = 0;
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.a=a, .s=s, .counter=(&counter)};
  const constant array<half4x4, 4>* const p_a = tint_module_vars.a;
  const constant half4x4* const p_a_i = (&(*p_a)[min(uint(i(tint_module_vars)), 3u)]);
  const constant half4* const p_a_i_i = (&(*p_a_i)[min(uint(i(tint_module_vars)), 3u)]);
  array<half4x4, 4> const l_a = (*p_a);
  half4x4 const l_a_i = (*p_a_i);
  half4 const l_a_i_i = (*p_a_i_i);
  (*tint_module_vars.s) = ((((*p_a_i_i).x + l_a[0][0].x) + l_a_i[0].x) + l_a_i_i.x);
}
