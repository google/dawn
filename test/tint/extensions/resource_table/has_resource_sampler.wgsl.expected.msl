#include <metal_stdlib>
using namespace metal;

struct tint_resource_table_metadata_struct {
  /* 0x0000 */ uint array_length;
  /* 0x0004 */ array<uint, 1> bindings;
};

struct tint_module_vars_struct {
  device uint* o;
  const device tint_resource_table_metadata_struct* tint_resource_table_metadata;
};

fragment void fs(device uint* o [[buffer(0)]], const device tint_resource_table_metadata_struct* tint_resource_table_metadata [[buffer(29)]]) {
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.o=o, .tint_resource_table_metadata=tint_resource_table_metadata};
  bool v = false;
  if ((0u < (*tint_module_vars.tint_resource_table_metadata).array_length)) {
    v = any((uint2((*tint_module_vars.tint_resource_table_metadata).bindings[0u]) == uint2(40u, 41u)));
  } else {
    v = false;
  }
  if (v) {
    (*tint_module_vars.o) = ((*tint_module_vars.o) + 1u);
  }
  bool v_1 = false;
  if ((1u < (*tint_module_vars.tint_resource_table_metadata).array_length)) {
    v_1 = ((*tint_module_vars.tint_resource_table_metadata).bindings[1u] == 42u);
  } else {
    v_1 = false;
  }
  if (v_1) {
    (*tint_module_vars.o) = ((*tint_module_vars.o) + 1u);
  }
}
