#include <metal_stdlib>
using namespace metal;

struct tint_module_vars_struct {
  threadgroup array<int, 246>* W;
};

struct tint_symbol_1 {
  array<int, 246> tint_symbol;
};

void main_inner(uint tint_local_index, tint_module_vars_struct tint_module_vars) {
  {
    uint v = 0u;
    v = tint_local_index;
    while(true) {
      uint const v_1 = v;
      if ((v_1 >= 246u)) {
        break;
      }
      (*tint_module_vars.W)[v_1] = 0;
      {
        v = (v_1 + 1u);
      }
    }
  }
  (threadgroup_barrier(mem_flags::mem_threadgroup));
  threadgroup array<int, 246>* const p = tint_module_vars.W;
  (*p)[0] = 42;
}

[[max_total_threads_per_threadgroup(1)]]
kernel void v_2(uint tint_local_index [[thread_index_in_threadgroup]], threadgroup tint_symbol_1* v_3 [[threadgroup(0)]]) {
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.W=(&(*v_3).tint_symbol)};
  (main_inner(tint_local_index, tint_module_vars));
}
