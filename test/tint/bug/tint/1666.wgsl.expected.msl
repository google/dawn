#include <metal_stdlib>
using namespace metal;

struct tint_array_lengths_struct {
  uint tint_array_length_0_0;
};

struct tint_immediate_data_struct {
  /* 0x0000 */ uint tint_non_constant_zero;
  /* 0x0004 */ array<int8_t, 2044> tint_pad;
  /* 0x0800 */ array<uint, 1> tint_storage_buffer_sizes;
};

struct tint_module_vars_struct {
  const device array<float, 1>* rarr;
  const constant tint_immediate_data_struct* tint_immediate_data;
};

void vector() {
  int const idx = 3;
  int const x = int2(1, 2)[min(uint(idx), 1u)];
}

void v() {
  int const idx = 4;
  float2 const x = float2x2(float2(1.0f, 2.0f), float2(3.0f, 4.0f))[min(uint(idx), 1u)];
}

void fixed_size_array() {
  array<int, 2> const arr = array<int, 2>{1, 2};
  int const idx = 3;
  int const x = arr[min(uint(idx), 1u)];
}

void runtime_size_array(tint_array_lengths_struct tint_array_lengths, tint_module_vars_struct tint_module_vars) {
  int const idx = -1;
  float const x = (*tint_module_vars.rarr)[min(uint(idx), (tint_array_lengths.tint_array_length_0_0 - 1u))];
}

[[max_total_threads_per_threadgroup(1)]]
kernel void f(const device array<float, 1>* rarr [[buffer(0)]], const constant tint_immediate_data_struct* tint_immediate_data [[buffer(30)]]) {
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.rarr=rarr, .tint_immediate_data=tint_immediate_data};
  tint_array_lengths_struct const v_1 = tint_array_lengths_struct{.tint_array_length_0_0=((*tint_module_vars.tint_immediate_data).tint_storage_buffer_sizes[0u] / 4u)};
  (vector());
  (v());
  (fixed_size_array());
  (runtime_size_array(v_1, tint_module_vars));
}
