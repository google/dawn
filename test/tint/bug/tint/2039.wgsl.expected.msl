#include <metal_stdlib>

using namespace metal;
kernel void tint_symbol() {
  uint out = 0u;
  bool tint_continue = false;
  while(true) {
    tint_continue = false;
    switch(2) {
      case 1: {
        tint_continue = true;
        break;
      }
      default: {
        break;
      }
    }
    if (tint_continue) {
      {
        if (true) { break; }
      }
      continue;
    }
    out = (out + 1u);
    {
      if (true) { break; }
    }
  }
  return;
}

