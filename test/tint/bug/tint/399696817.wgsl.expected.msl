#include <metal_stdlib>
using namespace metal;

struct tint_module_vars_struct {
  threadgroup atomic_int* a;
};

struct tint_symbol_1 {
  atomic_int tint_symbol;
};

void foo(uint in, tint_module_vars_struct tint_module_vars) {
  uint4 const v = uint4(24u, 16u, 8u, 0u);
  int4 const v_1 = as_type<int4>((uint4(in) << v));
  int4 const x = (v_1 >> uint4(24u));
  uint4 const v_2 = uint4(0u, 8u, 16u, 24u);
  uint4 const v_3 = (uint4(in) >> v_2);
  uint4 const y = (v_3 & uint4(255u));
  int const z = atomic_load_explicit(tint_module_vars.a, memory_order_relaxed);
}

void main_inner(uint tint_local_index, tint_module_vars_struct tint_module_vars) {
  if ((tint_local_index < 1u)) {
    atomic_store_explicit(tint_module_vars.a, 0, memory_order_relaxed);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  foo(1u, tint_module_vars);
}

kernel void v_4(uint tint_local_index [[thread_index_in_threadgroup]], threadgroup tint_symbol_1* v_5 [[threadgroup(0)]]) {
  tint_module_vars_struct const tint_module_vars = tint_module_vars_struct{.a=(&(*v_5).tint_symbol)};
  main_inner(tint_local_index, tint_module_vars);
}
