#include <metal_stdlib>
using namespace metal;

[[max_total_threads_per_threadgroup(1)]]
kernel void foo() {
  array<int, 2> explicitStride = {};
  array<int, 2> implictStride = {};
  implictStride = explicitStride;
}
