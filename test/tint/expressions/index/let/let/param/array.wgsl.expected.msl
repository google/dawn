#include <metal_stdlib>
using namespace metal;

int f(int x) {
  array<int, 8> const a = array<int, 8>{1, 2, 3, 4, 5, 6, 7, 8};
  int const i = x;
  return a[min(uint(i), 7u)];
}

[[max_total_threads_per_threadgroup(1)]]
kernel void v() {
  (f(1));
}
