#include <metal_stdlib>
using namespace metal;

void c(int z) {
  int a = as_type<int>((1u + as_type<uint>(z)));
  a = as_type<int>((as_type<uint>(a) + 2u));
}

[[max_total_threads_per_threadgroup(1)]]
kernel void b() {
  (c(2));
  (c(3));
}
